{
  "name": "devsos-dev-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Dev Client Web — ferramenta interna de desenvolvimento/teses do devSOS (consome o backend REST + WebSocket).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@stomp/stompjs": "^7.3.0",
    "sockjs-client": "^1.6.1"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
